marklar main() {
	marklar sumOfSquares = 0;
	marklar squareOfSum = 0;

	marklar sum = 0;
	marklar i = 1;

	while (i < 101) {
		sumOfSquares = sumOfSquares + (i * i);
		sum = sum + i;

		i = i + 1;
	}

	squareOfSum = sum * sum;
	
	return (squareOfSum - sumOfSquares) >> 0;
}

